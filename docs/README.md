# Docs

This folder will contain:
- `architecture.png` and `architecture.drawio` (diagram sources)
- `lineage.png` for data lineage (generated later)

Placeholders are added; to be completed in later milestones.